% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_rect_inventory.R
\name{create_rect_inventory}
\alias{create_rect_inventory}
\title{Make the inventory polygon rectangle and optionally axis-align it}
\usage{
create_rect_inventory(
  core_polygon_df,
  trees,
  north2x,
  sensors = NULL,
  rotate_axisaligned = TRUE
)
}
\arguments{
\item{core_polygon_df}{A validated data.frame defining the inventory polygon
with columns \code{x} and \code{y}.}

\item{trees}{A data.frame containing tree coordinates (\code{x}, \code{y}).}

\item{north2x}{Numeric. Clockwise angle from North to the X-axis (degrees).}

\item{sensors}{Optional data.frame containing sensor coordinates (\code{x}, \code{y}), or \code{NULL}.}

\item{rotate_axisaligned}{Logical. If \code{TRUE}, rotate coordinates to align rectangle with axes.}
}
\value{
A list with elements:
\describe{
\item{\code{core_polygon_df}}{Rectangular polygon (rotated axis-aligned if requested).}
\item{\code{trees}}{Updated tree data.frame (rotated if requested).}
\item{\code{sensors}}{Updated sensor data.frame (rotated if requested, or NULL).}
\item{\code{north2x}}{Updated orientation angle (degrees).}
\item{\code{rotation}}{Applied rotation angle in degrees. 0 if no rotation.}
}
}
\description{
The minimum-area enclosing rectangle is computed, and if
\code{rotate_axisaligned = TRUE}, align the rectangle with the X and Y axes, and
consequently, coordinates of trees (and sensors) are rotated.
}
\details{
Tree and sensor coordinates are rotated consistently.
}
\keyword{internal}
