% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_inventory.R
\name{plot_inventory}
\alias{plot_inventory}
\title{Plot a from-above view of a tree inventory}
\usage{
plot_inventory(trees_inv, transparency = TRUE, show_id = TRUE)
}
\arguments{
\item{trees_inv}{A data.frame of trees that passed \link{check_inventory}.
Must contain at least:
\itemize{
\item{\code{x}, \code{y}}{Tree coordinates (meters).}
\item{\code{rn_m}, \code{rs_m}, \code{re_m}, \code{rw_m}}{Crown radii (meters).}
\item{\code{h_m}}{Total height (meters) for plotting order.}
\item{\code{id_tree}}{Optional if show_id is FALSE; Tree identifier used for labeling.}
\item{\code{species}}{Optional; species name (character) for coloring.}
}}

\item{transparency}{Logical; if TRUE (default), uses alpha = 0.6, otherwise alpha = 1 (opaque).}

\item{show_id}{Logical; if TRUE (default), displays tree identifiers at crown centers.}
}
\value{
A ggplot object displaying the trees as ellipses in a from-above view.
}
\description{
Visualizes a forest inventory as ellipses representing tree crowns.
Taller trees are drawn above shorter ones. Optionally colors trees by species.
}
\details{
Time for plotting can be long because trees are plotted one by one in order to respect height
layering for both crowns and labels.
}
