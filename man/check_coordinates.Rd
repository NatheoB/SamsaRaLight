% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_coordinates.R
\name{check_coordinates}
\alias{check_coordinates}
\title{Check coordinate columns and determine whether conversion is required}
\usage{
check_coordinates(df, verbose = TRUE)
}
\arguments{
\item{df}{A data.frame containing spatial coordinates.}

\item{verbose}{Logical. If TRUE, informative messages are printed.}
}
\value{
Logical. Invisibly returns \code{TRUE} if coordinates must be converted
from longitude/latitude to planar coordinates, and \code{FALSE}
if planar coordinates are already present.
}
\description{
This function checks whether a data frame contains valid planar
coordinates (\code{x}, \code{y}) or geographic coordinates (\code{lon}, \code{lat}).
If geographic coordinates are detected, the user is informed that
they must be converted to a planar coordinate system using
\code{create_xy_from_lonlat()}.
}
\examples{
df_xy <- data.frame(x = 1:3, y = 4:6)
check_coordinates(df_xy)

df_lonlat <- data.frame(lon = c(10, 11), lat = c(45, 46))
check_coordinates(df_lonlat, verbose = TRUE)

}
